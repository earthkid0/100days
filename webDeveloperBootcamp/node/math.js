// const add = (x, y) => x + y;
// const square = (x) => x * x;

// exports.add = add; // module.exports
// exports.square = square;

module.exports = {
    add: (x, y) => x + y,
    square: (x) => x * x,
}